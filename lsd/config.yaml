blocks:
  - name
  - size
  - date
color:
   when: auto
   theme: custom
date: '+%d %b %y %H:%M'
size: short
sorting:
  dir-grouping: first
dereference: true
layout: oneline
icons:
  when: always
  theme: fancy
  separator: "  "
