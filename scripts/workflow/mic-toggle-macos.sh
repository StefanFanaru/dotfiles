#!/bin/bash

# Specify the microphone name (adjust as necessary for your setup)
MIC_NAME="HyperX SoloCast" # You can change this to the exact name of your microphone device

SwitchAudioSource -m toggle -t input
